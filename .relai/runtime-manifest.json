{
  "schema_version": "relai.runtime_manifest.v1",
  "env": [
    {
      "name": "OPENAI_API_KEY",
      "required": true,
      "sensitive": true,
      "reason": "The simulator runs the airline support agent through the OpenAI Agents SDK against the OpenAI API."
    }
  ],
  "files": []
}
