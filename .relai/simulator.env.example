# Set at least one provider key before running the simulator.
# If both are set, OPENAI_API_KEY takes precedence in this project.
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
